#include <stdio.h>
#include "main.h"
/**
 * mul- multiplica 2 enteros
 * @a: variable de entrada
 * @b: otra variable
 * Return: resultado
 */
int mul(int a, int b)
{
	int result = (a * b);
		return (result);
}
