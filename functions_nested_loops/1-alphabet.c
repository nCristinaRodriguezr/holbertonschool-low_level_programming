#include <stdio.h>
#include "main.h"
/**
 * main- imprimirá _putchar
 * Return: siempre devuelve 0
 */
void print_alphabet(void)
{
	char i;
	    for (i = 'a'; i <= 'z'; i++)
		            putchar(i);
}
