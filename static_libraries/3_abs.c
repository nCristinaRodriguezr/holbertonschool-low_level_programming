#include <stdlib.h>
#include "main.h"
#include <ctype.h>
/**
 * _abs- devuelve el absoluto de un numero
 * @n: variable de entrada
 * Return: el valor absoluto
 */
int _abs(int n)
{
	return (abs(n));
}
